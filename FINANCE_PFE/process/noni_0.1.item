<?xml version="1.0" encoding="UTF-8"?>
<talendfile:ProcessType xmi:version="2.0" xmlns:xmi="http://www.omg.org/XMI" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:TalendMapper="http://www.talend.org/mapper" xmlns:talendfile="platform:/resource/org.talend.model/model/TalendFile.xsd" defaultContext="Default" jobType="Standard">
  <context confirmationNeeded="false" hide="false" name="Default"/>
  <parameters>
    <elementParameter field="CLOSED_LIST" name="LOG4J_RUN_LEVEL" value="Info" show="false"/>
    <elementParameter field="CHECK" name="CATCH_RUNTIME_ERRORS" value="true" show="false"/>
  </parameters>
  <node componentName="tFileInputExcel" componentVersion="0.102" offsetLabelX="0" offsetLabelY="0" posX="120" posY="440">
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="tFileInputExcel_1" show="false"/>
    <elementParameter field="CHECK" name="VERSION_2007" value="true"/>
    <elementParameter field="FILE" name="FILENAME" value="D:/Data/status.xlsx"/>
    <elementParameter field="CHECK" name="ALL_SHEETS" value="true"/>
    <elementParameter field="TABLE" name="SHEETLIST">
      <elementValue elementRef="SHEETNAME" value="Sheet1" id="0"/>
    </elementParameter>
    <elementParameter field="TEXT" name="HEADER" value="1"/>
    <elementParameter field="TEXT" name="FOOTER" value="0"/>
    <elementParameter field="CHECK" name="DIE_ON_ERROR" value="false"/>
    <elementParameter field="CHECK" name="ADVANCED_SEPARATOR" value="true"/>
    <elementParameter field="TEXT" name="THOUSANDS_SEPARATOR" value="','"/>
    <elementParameter field="TEXT" name="DECIMAL_SEPARATOR" value="'.'"/>
    <elementParameter field="CHECK" name="TRIMALL" value="false"/>
    <elementParameter field="ENCODING_TYPE" name="ENCODING" value="UTF-8"/>
    <elementParameter field="TEXT" name="LABEL" value="statuus"/>
    <metadata connector="FLOW" label="metadata" name="tFileInputExcel_1">
            <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="SEKCONTR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="KEKCONTR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="KFRORDNR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DTSTATCHG" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DM_EXDATE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="RECORDMODE" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DSFRUSER" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DESCRIPTION" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
    </metadata>
    <metadata connector="REJECT" label="metadata" name="REJECT">
            <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="SEKCONTR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="KEKCONTR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="KFRORDNR" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DTSTATCHG" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DM_EXDATE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="RECORDMODE" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DSFRUSER" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if col == "SEKCONTR" else "12" if col == "KEKCONTR" else "10" if col == "KFRORDNR" else "-1" if col in ["DTSTATCHG", "DM_EXDATE"] else "0" if col == "RECORDMODE" else "21" if col == "DSFRUSER" else "39" if col == "DESCRIPTION" else "255"}" name="DESCRIPTION" nullable="true" pattern="dd-MM-yyyy" precision="0" type="id_String" usefulColumn="true"/>
      <column defaultValue="" key="false" length="255" name="errorCode" nullable="true" precision="0" type="id_String" usefulColumn="true"/>
      <column defaultValue="" key="false" length="255" name="errorMessage" nullable="true" precision="0" type="id_String" usefulColumn="true"/>
    </metadata>
  </node>
  <node componentName="tMap" componentVersion="2.1" offsetLabelX="0" offsetLabelY="0" posX="280" posY="440">
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="tMap_1" show="false"/>
    <elementParameter field="CHECK" name="DIE_ON_ERROR" value="true"/>
    <metadata connector="FLOW" label="status" name="status">
            <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="STATUS_OF_EKIP_CONTRACT" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="CONTRACT_ID" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="ORDER_NUMBER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DATE_OF_STATUS_CHANGE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="EXTRACTION_DATE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="USER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DESCRIPTION" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
    </metadata>
    <nodeData xsi:type="TalendMapper:MapperData">
      <uiProperties shellMaximized="true"/>
      <varTables sizeState="INTERMEDIATE" name="Var" minimized="true"/>
      <outputTables sizeState="INTERMEDIATE" name="status">
                <mapperTableEntries name="STATUS_OF_EKIP_CONTRACT" expression="row1.SEKCONTR" type="id_String" nullable="true"/>
        <mapperTableEntries name="CONTRACT_ID" expression="row1.KEKCONTR" type="id_String" nullable="true"/>
        <mapperTableEntries name="ORDER_NUMBER" expression="row1.KFRORDNR" type="id_String" nullable="true"/>
        <mapperTableEntries name="DATE_OF_STATUS_CHANGE" expression="row1.DTSTATCHG" type="id_Date" nullable="true"/>
        <mapperTableEntries name="EXTRACTION_DATE" expression="row1.DM_EXDATE" type="id_Date" nullable="true"/>
        <mapperTableEntries name="USER" expression="row1.DSFRUSER" type="id_String" nullable="true"/>
        <mapperTableEntries name="DESCRIPTION" expression="row1.DESCRIPTION" type="id_String" nullable="true"/>
      </outputTables>
      <inputTables sizeState="INTERMEDIATE" name="row1" matchingMode="UNIQUE_MATCH" lookupMode="LOAD_ONCE">
                <mapperTableEntries name="SEKCONTR" type="id_String" nullable="true"/>
        <mapperTableEntries name="KEKCONTR" type="id_String" nullable="true"/>
        <mapperTableEntries name="KFRORDNR" type="id_String" nullable="true"/>
        <mapperTableEntries name="DTSTATCHG" type="id_Date" nullable="true"/>
        <mapperTableEntries name="DM_EXDATE" type="id_Date" nullable="true"/>
        <mapperTableEntries name="RECORDMODE" type="id_String" nullable="true"/>
        <mapperTableEntries name="DSFRUSER" type="id_String" nullable="true"/>
        <mapperTableEntries name="DESCRIPTION" type="id_String" nullable="true"/>
      </inputTables>
    </nodeData>
  </node>
  <node componentName="tDBOutput" componentVersion="0.1" offsetLabelX="0" offsetLabelY="0" posX="440" posY="440">
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="tDBOutput_1" show="false"/>
    <elementParameter field="TEXT" name="DB_VERSION" value="Snowflake"/>
    <elementParameter field="TEXT" name="URL" value=""jdbc:snowflake://VSKIQIR-UE58575.snowflakecomputing.com/?user=OUMAYMADEBBICH1&warehouse=COMPUTE_WH&db=BANQUE&schema=PFE""/>
    <elementParameter field="TEXT" name="USER" value="OUMAYMADEBBICH1"/>
    <elementParameter field="PASSWORD" name="PASS" value="Adem123456789@"/>
    <elementParameter field="TEXT" name="TABLE" value="SA_STATUUUS"/>
    <elementParameter field="CLOSED_LIST" name="TABLE_ACTION" value="NONE"/>
    <elementParameter field="CLOSED_LIST" name="DATA_ACTION" value="INSERT"/>
    <elementParameter field="CHECK" name="DIE_ON_ERROR" value="false"/>
    <elementParameter field="CHECK" name="USE_BATCH_SIZE" value="true"/>
    <elementParameter field="TEXT" name="BATCH_SIZE" value="10000"/>
    <metadata connector="FLOW" label="flow" name="tDBOutput_1">
            <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="STATUS_OF_EKIP_CONTRACT" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="CONTRACT_ID" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="ORDER_NUMBER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DATE_OF_STATUS_CHANGE" nullable="true" pattern="yyyy-MM-dd" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="EXTRACTION_DATE" nullable="true" pattern="yyyy-MM-dd" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="USER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DESCRIPTION" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
    </metadata>
    <metadata connector="REJECT" label="reject" name="reject">
            <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="STATUS_OF_EKIP_CONTRACT" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="CONTRACT_ID" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="ORDER_NUMBER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DATE_OF_STATUS_CHANGE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="EXTRACTION_DATE" nullable="true" pattern="dd/MM/yyyy" precision="0" type="id_Date" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="USER" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" key="false" length="{"4" if m["target_field"] == "STATUS_OF_EKIP_CONTRACT" else "12" if m["target_field"] == "CONTRACT_ID" else "10" if m["target_field"] == "ORDER_NUMBER" else "-1" if m["target_field"] in ["DATE_OF_STATUS_CHANGE", "EXTRACTION_DATE"] else "60" if m["target_field"] == "USER" else "100" if m["target_field"] == "DESCRIPTION" else "255"}" name="DESCRIPTION" nullable="true" pattern="" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" defaultValue="" key="false" length="255" name="errorCode" nullable="true" precision="0" type="id_String" usefulColumn="true"/>
      <column comment="" defaultValue="" key="false" length="255" name="errorMessage" nullable="true" precision="0" type="id_String" usefulColumn="true"/>
    </metadata>
  </node>
  <connection connectorName="FLOW" label="row1" lineStyle="0" metaname="tFileInputExcel_1" offsetLabelX="0" offsetLabelY="0" source="tFileInputExcel_1" target="tMap_1">
    <elementParameter field="CHECK" name="MONITOR_CONNECTION" value="false"/>
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="row1" show="false"/>
  </connection>
  <connection connectorName="FLOW" label="status" lineStyle="0" metaname="status" offsetLabelX="0" offsetLabelY="0" source="tMap_1" target="tDBOutput_1">
    <elementParameter field="CHECK" name="MONITOR_CONNECTION" value="false"/>
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="status" show="false"/>
  </connection>
  <subjob>
    <elementParameter field="TEXT" name="UNIQUE_NAME" value="tFileInputExcel_1" show="false"/>
  </subjob>
</talendfile:ProcessType>